# Frontend Development Configuration

# Backend API Port (for development mode only)
# In production, the frontend is served by the backend and uses the same origin
# Default: 5026
VITE_API_PORT=5026
